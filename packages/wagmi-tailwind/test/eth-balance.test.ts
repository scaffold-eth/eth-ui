import 'test/helpers/chai-imports';
import { expect } from 'chai';

describe('useEthBalance', function () {
  it('When a test is run mocha and chai work', () => {
    expect(true).be.equal(true);
  });
});
